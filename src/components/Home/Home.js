import {React, Fragment} from "react";
import UsersList from "./Users/UsersList/UsersList";

const Home = props => {
  return (
    <Fragment>
      <UsersList />
    </Fragment>
  );
};

export default Home;
