import React from "react";
import Button from "../../../UI/Button";
import classes from './AddUser.module.scss';

const AddUser = (props) => {
  return <Button type="button" className={classes["btn-supernova"]}>Add User</Button>;
};

export default AddUser;
