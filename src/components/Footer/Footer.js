import { React } from "react";
import classes from "./Footer.module.scss";

const Footer = (props) => {
  return (
    <footer className={classes.footer}>
      <h1>React Project - Onur Kanca</h1>
    </footer>
  );
};

export default Footer;
